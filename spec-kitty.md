# Spec Kitty - Посібник користувача

> **Джерело**: [Priivacy-ai/spec-kitty на GitHub](https://github.com/Priivacy-ai/spec-kitty)  
> **Версія**: 0.10.6+  
> **Дата оновлення**: 2026-01-06

---

## 📌 Що таке Spec Kitty?

**Spec Kitty** - це платформа для автоматизації розробки з підтримкою багатьох AI-агентів. Вона допомагає організувати роботу над проектом через spec-driven підхід з візуальним відстеженням прогресу.

### Основні переваги

✅ Real-time kanban dashboard для відстеження роботи  
✅ Підтримка 12+ AI агентів (Claude, Cursor, Gemini, Copilot, Windsurf тощо)  
✅ Паралельна робота над різними функціями через worktrees  
✅ Автоматизоване управління задачами  
✅ Система стандартів команди (constitution framework)

---

## 🚀 Швидкий старт

### Крок 1: Встановлення

```bash
# Встановити CLI
pip install spec-kitty-cli
# або
uv tool install spec-kitty-cli
```

### Крок 2: Створення проекту

```bash
# Створити новий проект
spec-kitty init my-project --ai claude

# Або в поточній директорії
spec-kitty init . --ai gemini

# Перейти до проекту
cd my-project
```

**Що створюється:**
- `.claude/commands/` або `.gemini/commands/` - slash-команди для AI агента
- `.kittify/` - конфігурація, шаблони та скрипти
- `.gitignore` та git репозиторій

**Підтримувані AI агенти:**
`claude`, `gemini`, `copilot`, `cursor`, `windsurf`, `qwen`, `opencode`, `codex`, `kilocode`, `auggie`, `roo`, `q`

### Крок 3: Запуск dashboard

```bash
spec-kitty dashboard
# Відкриється на http://localhost:3000-5000
```

---

## 📋 Послідовність роботи з проектом

### 🎯 На початку проекту (один раз)

#### 1. Визначте принципи проекту

```
В AI агенті виконайте:
/spec-kitty.constitution
```

Це створить файл з правилами кодування, стандартами та принципами вашого проекту.

---

### 🔄 Для кожної нової функції

#### 2. Створіть специфікацію

```
/spec-kitty.specify
```

**Що відбувається:**
- Створюється опис функції (feature spec)
- Автоматично створюється feature branch
- Створюється окремий worktree в `.worktrees/XXX-feature`

**Наступний крок:**
```bash
cd .worktrees/001-my-feature
```

#### 3. Розробіть технічний план

```
/spec-kitty.plan
```

Створює детальний технічний план реалізації функції.

**Опціонально:** Якщо потрібне додаткове дослідження:
```
/spec-kitty.research
```

#### 4. Згенеруйте робочі задачі

```
/spec-kitty.tasks
```

Створює work packages (WP01, WP02, ...) - набори підзадач для реалізації.

**Опціонально:** Перевірте узгодженість документів:
```
/spec-kitty.analyze
```

#### 5. Реалізуйте функцію

```
/spec-kitty.implement WP01
```

Повторюйте для кожного work package. AI агент автоматично:
- Переміщує задачу в статус "doing"
- Показує інструкції для виконання
- Відстежує прогрес у dashboard

#### 6. Перегляньте результат

```
/spec-kitty.review WP01
```

Перевірте виконану роботу перед фінальною валідацією.

#### 7. Валідуйте готовність

```
/spec-kitty.accept
```

Система перевірить:
- Чи всі задачі виконані
- Чи дотримано стандартів якості
- Чи готово до merge

#### 8. Об'єднайте з основною гіллою

```
/spec-kitty.merge
```

**Що відбувається:**
- Злиття feature branch в main
- Видалення worktree
- Cleanup тимчасових файлів

---

## 🛠️ Корисні команди

### Моніторинг проекту

```bash
# Відкрити dashboard
spec-kitty dashboard

# Показати стан проекту
spec-kitty diagnostics

# Список всіх задач
spec-kitty agent tasks list-tasks
```

### Управління задачами

```bash
# Перемістити задачу між статусами
spec-kitty agent tasks move-task WP01 --to doing

# Позначити статус задачі
spec-kitty agent tasks mark-status WP01 --status done

# Перевірити метадані задачі
spec-kitty agent tasks validate-workflow WP01
```

### Оновлення проекту

```bash
# Перевірити що буде оновлено
spec-kitty upgrade --dry-run

# Виконати оновлення
spec-kitty upgrade

# З детальним виводом
spec-kitty upgrade --verbose
```

---

## 📚 Основні поняття

| Термін | Опис |
|--------|------|
| **Project** | Вся кодова база вашого програмного забезпечення |
| **Feature** | Окрема функціональність, що розробляється ізольовано |
| **Worktree** | Окрема робоча копія для feature (в `.worktrees/`) |
| **Work Package (WP)** | Набір пов'язаних підзадач (формат: WP01, WP02...) |
| **Task** | Конкретна підзадача в межах Work Package |
| **Lane** | Статус задачі: `planned`, `doing`, `for_review`, `done` |
| **Mission** | Тип workflow: `software-dev` (розробка) або `research` (дослідження) |

---

## 💡 Додаткові slash-команди

| Команда | Коли використовувати |
|---------|---------------------|
| `/spec-kitty.clarify` | Перед плануванням - поставити уточнюючі питання |
| `/spec-kitty.checklist` | Створити custom чеклист якості |
| `/spec-kitty.dashboard` | Відкрити або перезапустити dashboard |

---

## 🎯 Типовий робочий процес

```mermaid
graph TD
    A[spec-kitty init] --> B[/spec-kitty.constitution]
    B --> C[/spec-kitty.specify]
    C --> D[cd .worktrees/XXX]
    D --> E[/spec-kitty.plan]
    E --> F[/spec-kitty.tasks]
    F --> G[/spec-kitty.implement WP01]
    G --> H{Є ще WP?}
    H -->|Так| G
    H -->|Ні| I[/spec-kitty.review]
    I --> J[/spec-kitty.accept]
    J --> K[/spec-kitty.merge]
    K --> L{Нова feature?}
    L -->|Так| C
    L -->|Ні| M[Готово]
```

---

## 🔧 Приклади використання

### Приклад 1: Новий проект з Claude

```bash
# Створити проект
spec-kitty init my-app --ai claude
cd my-app

# Відкрити Claude і виконати:
# /spec-kitty.constitution
# Описати принципи проекту

# Запустити dashboard
spec-kitty dashboard
```

### Приклад 2: Додати нову функцію

```bash
# В AI агенті:
# /spec-kitty.specify
# Описати: "Додати форму реєстрації користувача"

# Перейти до worktree
cd .worktrees/001-registration-form

# Продовжити workflow:
# /spec-kitty.plan
# /spec-kitty.tasks
# /spec-kitty.implement WP01
# /spec-kitty.implement WP02
# /spec-kitty.review
# /spec-kitty.accept
# /spec-kitty.merge
```

### Приклад 3: Робота з існуючим проектом

```bash
# Оновити spec-kitty до нової версії
spec-kitty upgrade

# Відкрити dashboard
spec-kitty dashboard

# Перевірити стан
spec-kitty diagnostics

# Подивитись активні задачі
spec-kitty agent tasks list-tasks
```

---

## ❓ Часті питання

**Q: Як працювати над кількома функціями одночасно?**  
A: Кожна функція має свій worktree. Просто виконайте `/spec-kitty.specify` для кожної функції і працюйте в окремих директоріях `.worktrees/`.

**Q: Що робити якщо щось пішло не так?**  
A: Використайте `spec-kitty diagnostics` для діагностики та `spec-kitty verify-setup` для перевірки налаштувань.

**Q: Чи можна використовувати без AI агента?**  
A: Технічно так, але інструмент призначений для роботи з AI агентами. Можна виконувати CLI команди вручну.

**Q: Як перемикатись між AI агентами?**  
A: При ініціалізації вкажіть потрібного агента через `--ai`. Можна мати кілька агентів одночасно.

---

## 📖 Додаткові ресурси

- [GitHub Repository](https://github.com/Priivacy-ai/spec-kitty)
- [Повна документація](https://github.com/Priivacy-ai/spec-kitty/blob/main/spec-driven.md)
- [Історія змін](https://github.com/Priivacy-ai/spec-kitty/blob/main/CHANGELOG.md)

---

## 📝 Короткий довідник команд

### CLI команди
```bash
spec-kitty init <project>          # Створити проект
spec-kitty dashboard               # Відкрити dashboard
spec-kitty upgrade                 # Оновити проект
spec-kitty diagnostics             # Діагностика
spec-kitty agent tasks list-tasks  # Список задач
```

### Slash-команди (в AI агенті)
```
/spec-kitty.constitution  # Встановити принципи (1 раз)
/spec-kitty.specify       # Нова функція
/spec-kitty.plan          # Технічний план
/spec-kitty.tasks         # Створити WP
/spec-kitty.implement     # Реалізувати WP
/spec-kitty.review        # Перегляд
/spec-kitty.accept        # Валідація
/spec-kitty.merge         # Об'єднання
```

---

**Успішної роботи з Spec Kitty! 🐱**
