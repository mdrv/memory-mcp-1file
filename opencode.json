{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "memory-mcp": {
      "type": "local",
      "command": ["docker", "run", "--rm", "-i", "-v", "memory-mcp-data:/data", "memory-mcp:dev"],
      "enabled": true
    }
  }
}
