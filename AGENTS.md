# Інструкції для AI агентів

> **Призначення**: Правила та обмеження для роботи AI агентів у цьому проекті

---

## 🚫 Обмеження роботи з репозиторіями

### Заборонено

**ЗАБОРОНЕНО** використовувати кореневу директорію проекту для підключення зовнішніх репозиторіїв.

### Дозволено

Для підключення зовнішніх репозиторіїв використовуйте виключно директорію **`_tmp/`**.

**Приклад:**
```bash
# ✅ Правильно
cd _tmp
git clone https://github.com/example/repo.git

# ❌ Неправильно
git clone https://github.com/example/repo.git
```

---

## 🗣️ Мова комунікації

**Спілкуйся з користувачем виключно українською мовою.**

Всі відповіді, пояснення та документація мають бути українською.

---

## 📦 Управління залежностями

### Встановлення пакетів

**Встановлюйте пакети ЛИШЕ через `cargo`, а НЕ через ручне редагування `Cargo.toml`.**

**Правильний підхід:**
```bash
# ✅ Правильно - використовуємо cargo add
cargo add serde
cargo add tokio --features full
cargo add serde_json --dev

# ✅ Оновлення пакетів
cargo update
cargo upgrade  # з cargo-edit
```

**Неправильний підхід:**
```bash
# ❌ Неправильно - ручне редагування Cargo.toml
# НЕ редагуйте Cargo.toml вручну для додавання залежностей
```

**Чому це важливо:**
- Автоматичне визначення останньої сумісної версії
- Правильне оновлення `Cargo.lock`
- Уникнення помилок синтаксису
- Автоматична перевірка існування пакету

---

## 📋 Підсумок правил

| Правило | Опис |
|---------|------|
| **Зовнішні репозиторії** | Тільки в `_tmp/` директорії |
| **Встановлення пакетів** | Використовувати `cargo add`, не редагувати `Cargo.toml` вручну |
| **Мова спілкування** | Виключно українська |

---

*Останнє оновлення: 2026-01-06*